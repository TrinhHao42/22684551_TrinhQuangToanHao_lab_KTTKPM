spring:
  application:
    name: shippingService

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: rootpassword
    url: jdbc:mariadb://localhost:3306/tuan05
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8083
