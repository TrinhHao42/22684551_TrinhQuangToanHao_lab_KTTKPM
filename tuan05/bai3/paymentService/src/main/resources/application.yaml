spring:
  application:
    name: paymentService
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: sapassword
    url: jdbc:mariadb://localhost:3306/tuan05
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8082